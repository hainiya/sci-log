{
  "name": "materials-research-copilot",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "科研工作台 v2 App（manifestVersion 2）。零构建直接交付：index.js 入口 + lib/ 数据层 + tools/ 工具 + ui/panel.html 静态面板。src-server/ 与 ui/*.tsx 为 v1 时代 TS 源码，仅作参考保留（见 v1-migration 前的 git 历史）。",
  "scripts": {
    "test": "node tests/run-all.mjs",
    "legacy:typecheck": "tsc --noEmit && tsc -p tsconfig.server.json",
    "legacy:test:ui": "vitest run",
    "legacy:build": "node scripts/sync-version.mjs && node scripts/install-sdk.mjs && npm run legacy:build:ui && npm run legacy:build:server",
    "legacy:build:ui": "vite build",
    "legacy:build:server": "esbuild src-server/index.ts src-server/tools/*.ts src-server/routes/*.ts --bundle --format=esm --platform=node --outbase=src-server --outdir=legacy-dist --log-level=warning --external:fs --external:http --external:https --external:url"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.3",
    "esbuild": "^0.28.1"
  }
}
